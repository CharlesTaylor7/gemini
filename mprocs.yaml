procs:
  Test:
    shell: yarn test --clear-screen --watch

  Dev Server:
    shell: yarn watch:js

  Tailwind:
    shell: yarn tailwind --watch

  Legacy Haskell App:
    cwd: haskell/
    shell: ./scripts/run-dev.sh
    autostart: false
