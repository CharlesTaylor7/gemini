procs:
  Purescript:
    shell: yarn watch:purs

  Dev Server:
    shell: yarn watch:js

  Format:
    shell: yarn format
    autostart: false

  Tailwind:
    shell: yarn tailwind --watch

  Legacy Haskell App:
    cwd: haskell/
    shell: ./scripts/run-dev.sh
    autostart: false
